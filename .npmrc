# Dependencies are externalized at build time (webpack externals); none need install
# lifecycle scripts. tabby-ssh's Windows postinstall (run-script-os) is broken upstream
# and hard-fails on win32, so skip dependency install scripts entirely.
ignore-scripts=true
